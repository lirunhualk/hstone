# AGENTS.md

- 人类掌舵，Codex 执行。
- 先读上下文，再修改；不要凭空假设。
- `AGENTS.md` 只做地图，不做百科全书；详细规则写入仓库文档。
- 大任务先拆小，优先交付可独立验证的结果。
- 遇到反复失败，先补文档、工具、测试或约束，不要只重复尝试。
- 在边界处校验数据；保持结构、命名和分层清晰稳定。
- 用 lint、测试、CI 和脚本强制执行关键规则，不依赖口头约定。
- 优先让 Codex 可直接读取测试结果、日志、终端输出、页面状态和截图。
- 把反馈沉淀为文档、脚本、测试或规则，减少同类问题重复出现。
- 输出以正确、可维护、对后续 Codex 清晰为准，不以“像人手写”作为目标。

## 完成修改后的 Git 发布流程

在本仓库中完成用户要求的修改后，默认执行以下流程，无需用户再次要求：

1. 检查 `git status` 和待提交差异，确认提交范围仅包含本次任务相关修改，不覆盖或混入用户的其他工作。
2. 检查待提交内容和提交元数据是否适合公开，包括但不限于密钥、Token、密码、私钥、个人邮箱、个人路径、内网地址、客户数据及其他敏感信息。
3. 运行与修改相称的 lint、测试、构建或其他可用验证。
4. 仅当检查和验证均通过时，创建内容明确、范围准确的 Git commit。
5. 确认当前分支及其上游安全且符合预期后，将该 commit push 到配置的远端。

出现以下任一情况时，不得自动 commit 或 push；应停止并向用户说明具体阻塞项：

- 发现或疑似包含敏感信息。
- 测试、lint、构建或必要验证失败。
- 变更中混有无法确认归属的用户修改。
- 当前处于 detached HEAD、上游缺失、远端或目标分支不明确。
- push 需要 force、会改写已发布历史、存在非快进冲突，或需要其他破坏性操作。
- 提交或 push 需要用户作出会显著改变结果的选择。

不得为了完成自动发布而跳过检查、降低测试要求、关闭保护措施、强制推送或擅自删除用户修改。
